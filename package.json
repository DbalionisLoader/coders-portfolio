{
  "name": "coders-portfolio",
  "version": "1.0.0",
  "description": "David Balionis Porfolio Website",
  "main": ".eleventy.js",
  "dependencies": {
    "@webpixels/css": "^1.2.6",
    "bootstrap": "^5.3.0",
    "three": "^0.177.0",
    "esbuild": "^0.25.5"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "eleventy-google-fonts": "^0.1.0",
    "netlify-cli": "^17.3.1",
    "parcel": "^2.9.3"
  },
  "scripts": {
    "build:js": "esbuild ./js/icon_render.js --bundle --minify --outfile=_site/js/icon_render.bundle.js",
    "build": "npm run build:js && eleventy",
    "serve": "npm run build:js && eleventy --serve", 
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
